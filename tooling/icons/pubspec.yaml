name: tawaq_icon_generator
description: Generate desktop and tray icons for Tawaq from a single PNG source.
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  args: ^2.7.0
  image: ^4.5.4
  path: ^1.9.1
