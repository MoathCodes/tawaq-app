# Drop your icon files here

- `logo.png` — used on macOS / Linux / Windows (GDI+).
- `logo.ico` — optional, best quality on Windows.
- `alt.png` — second icon used by the example to show live switching.
